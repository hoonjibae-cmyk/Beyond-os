# v41-26.1 — 데일리 리포트 발송 상태 배지 정리

## 기준선

- v41-26 weekly report UI polish

## 반영 내용

1. 데일리 리포트 발송 상태 카드의 `테스트 수신번호 적용` 배지를 큰 버튼 형태가 아닌 작은 상태 라벨로 정리했습니다.
2. 데일리 리포트와 위클리 리포트의 발송 상태 카드 UI 톤을 맞췄습니다.
3. 모바일/데스크탑 모두에서 발송 상태 배지가 카드 우측에 과하게 커 보이지 않도록 보정했습니다.

## 추가 설정

없음.

## 배포

```powershell
npx.cmd vercel --prod --yes
```
